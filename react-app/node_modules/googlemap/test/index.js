import assert from 'assert';
import GoogleMap from './../src/index';
import jsdom from 'mocha-jsdom';

describe('GoogleMap', () => {

  jsdom();

  it('does stuff', () => {
    let canvas = document.createElement('div');

    new GoogleMap(canvas);
  });

});
